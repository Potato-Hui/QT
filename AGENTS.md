# Repository Agent Rules

- Do not add standalone test source files or separate test targets for this project.
- Keep generated build trees and CMake output in ignored build directories.
- For UI changes, validate by building the relevant application or desktop preview and performing a focused smoke check.
